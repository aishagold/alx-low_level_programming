#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: the character to be checked
 * Return: Absolute number
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
